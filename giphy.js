class Giphy{

    constructor(keyword){
        this.keyboard = keyword; //guardar en lso atributos del objeto
        this.endponit = "endpoint"

    }

    static getUrlAsync(keyword,callback){ //devuelve la ulr del gif y de manera asincrona
        return new Giphy()
    } //esta funcion generar un objeto de la clase Giphy
}